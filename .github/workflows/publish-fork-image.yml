name: Publish Fork Docker Images

on:
  push:
    branches:
      - develop
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-fork-images-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    name: Publish Tandoor + MCP amd64/arm64 images
    if: github.repository == 'KingHacker9000/recipes'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: yarn
          cache-dependency-path: vue3/yarn.lock

      - name: Build Vue frontend
        working-directory: ./vue3
        run: |
          yarn install --frozen-lockfile
          yarn build

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tandoor image metadata
        id: tandoor_meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/kinghacker9000/recipes
          tags: |
            type=raw,value=develop
            type=sha,prefix=sha-,format=long
          labels: |
            org.opencontainers.image.source=https://github.com/KingHacker9000/recipes
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build and push Tandoor image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          pull: true
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.tandoor_meta.outputs.tags }}
          labels: ${{ steps.tandoor_meta.outputs.labels }}
          cache-from: type=gha,scope=tandoor
          cache-to: type=gha,mode=max,scope=tandoor

      - name: MCP image metadata
        id: mcp_meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/kinghacker9000/recipes-mcp
          tags: |
            type=raw,value=develop
            type=sha,prefix=sha-,format=long
          labels: |
            org.opencontainers.image.source=https://github.com/KingHacker9000/recipes
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Build and push MCP image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.mcp
          pull: true
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.mcp_meta.outputs.tags }}
          labels: ${{ steps.mcp_meta.outputs.labels }}
          cache-from: type=gha,scope=mcp
          cache-to: type=gha,mode=max,scope=mcp
